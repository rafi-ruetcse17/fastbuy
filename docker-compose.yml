version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    volumes:
      - ./server:/app
    depends_on:
      - client

  client:
    build: ./client
    ports:
      - "3000:3000"
    volumes:
      - ./client:/app
